## Sandels
### Learning raylib by creating a sand physics puzzle type game inspired by classic browser games, Sandels.

Built with C++20 using the default CLion MinGW toolchain, being G++, Ninja and CMake. <br>
The only and primary external library used is raylib v6.0, it should automatically be installed at compilation.